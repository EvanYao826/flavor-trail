spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flavor_trail?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key: ${DASHSCOPE_API_KEY:your-api-key}
      chat:
        options:
          model: qwen-plus
          temperature: 0.7

logging:
  level:
    com.flavor.trail: DEBUG
    org.mybatis: DEBUG